extends base
include mixins/video

block content
    h1 Home
    input(placeholder="Whats your name")
    button send
    each video in videos
        +video(video)
    else
        li Sorry nothing found.